const outputs = [];
//This is getting all ball detail
function onScoreUpdate(dropPosition, bounciness, size, bucketLabel) {
  outputs.push([dropPosition,bounciness,size,bucketLabel])
  console.log(outputs);
}

function runAnalysis() {
	
}

